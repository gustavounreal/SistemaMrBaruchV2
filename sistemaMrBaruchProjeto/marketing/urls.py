from django.urls import path
from . import views

app_name = 'marketing'

urlpatterns = [
    # Placeholder - adicionar views conforme necessário
    # path('leads/', views.listar_leads, name='listar_leads'),
    # path('leads/<int:lead_id>/', views.detalhes_lead, name='detalhes_lead'),
]
